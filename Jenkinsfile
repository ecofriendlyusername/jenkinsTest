pipeline {
    agent { 
        dockerfile {
            dir "./back"
        } 
    }
    stages {
        stage('Test') {
            steps {
                echo 'hello'
            }
        }
    }
}
